[package]
name = "zach"
version = "0.1.0"
edition = "2024"
description = "Restricted GitHub-mediated workspace control agent"
license = "MIT"

[dependencies]

[target.'cfg(target_os = "linux")'.dependencies]
libc = "0.2"
